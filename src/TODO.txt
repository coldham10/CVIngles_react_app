form on 'hidden' page, have to click through from cards giving option types
checkout page
new branch:  more OOP:
    each section a proper object
    keeps entry objects -each extends of CVEntry
    state kept in lowest level. - not relying on JSON except for load/save
    saving: toJSON defined recursively, as well as parse methods
